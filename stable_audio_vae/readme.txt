1. change data path in configs/vae_data.txt (any folder contains audio files)

2. change model config in configs/vae_16k_mono_v2.config

3. change batch size and training settings in defaults.ini

4. bash train_bash.sh 